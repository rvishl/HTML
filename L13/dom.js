//Alert something on click of the button.
function alertSomething(arg1) {
    // alert(arg1);
    var something = document.getElementById('abcd');
    // console.log(something);
    // console.log(typeof something);
    // something.innerText = "Magic.";
    something.innerText = "Something happen";
}

//Get an element from the HTML document.